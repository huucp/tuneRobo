<UserControl x:Class="TuneRoboWPF.Views.MotionItemVertical"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Width="125" Height="180">
    <Grid>
    	<Image x:Name="Artwork" Height="125" Margin="0" Width="125" VerticalAlignment="Top"
               Source="{Binding ArtworkImage, TargetNullValue={StaticResource DefaultImage}}"/>
    	<TextBlock x:Name="Title" VerticalAlignment="Bottom" Height="53"
               TextWrapping="Wrap" Margin="0,0,15,0">
            <Run Text="{Binding MotionTitle}" FontWeight ="Bold"/>
            <LineBreak/>
            <Run Text="{Binding ArtistName}" TextDecorations="Underline"/>
    	</TextBlock>
        <Label x:Name="Artist" Content="{Binding ArtistName}" Margin="1,0,15,12" VerticalAlignment="Bottom"
               Visibility="Collapsed"/>
            
    </Grid>
</UserControl>
