<UserControl
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:local="clr-namespace:TuneRoboWPF.Views"
	mc:Ignorable="d" x:Class="TuneRoboWPF.Views.RemoteControlScreen"
	d:DesignWidth="1100" d:DesignHeight="703"
    Loaded="UserControl_Loaded">

    <UserControl.Resources>
       
        <Style x:Key="CustomThumbForSlider" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Ellipse Fill="#FFf5f5f5" Stroke="#FFc3c3c3" Height="15" Width="15">
                            <Ellipse.Effect>
                                <DropShadowEffect Direction="270" BlurRadius="2" ShadowDepth="1" Color="#dddddd"/>
                            </Ellipse.Effect>
                        </Ellipse>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SliderRepeatButtonStyle" TargetType="{x:Type RepeatButton}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Rectangle Fill="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>        

        <Style x:Key="VolumeSliderStyle" TargetType="{x:Type Slider}">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Slider}">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TickBar x:Name="TopTick" Visibility="Collapsed" Fill="{TemplateBinding Foreground}" Placement="Top" Height="4" Grid.Row="0"/>
                                <TickBar x:Name="BottomTick" Visibility="Collapsed" Fill="{TemplateBinding Foreground}" Placement="Bottom" Height="4" Grid.Row="0"/>
                                <Border x:Name="TrackBackground" Background="Transparent"
                                        BorderBrush="Transparent"   
                                        BorderThickness="1" CornerRadius="1"
                                        Margin="5,0" VerticalAlignment="Center" Height="4.0" Grid.Row="1" >
                                    <Canvas Margin="-6,-1">
                                        <Rectangle Visibility="Hidden" x:Name="PART_SelectionRange" Height="4.0"
                                                   Fill="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"
                                                   Stroke="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}"
                                                   StrokeThickness="0.0"/>
                                        
                                    </Canvas>
                                </Border>
                                <Track x:Name="PART_Track" Grid.Row="1">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Style="{StaticResource SliderRepeatButtonStyle}"/>
                                    </Track.DecreaseRepeatButton>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Style="{StaticResource SliderRepeatButtonStyle}"/>
                                    </Track.IncreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb x:Name="Thumb" Style="{StaticResource CustomThumbForSlider}" Background="Transparent"/>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="VolumeStyle" TargetType="{x:Type ProgressBar}">
            <Setter Property="Foreground" Value="#FFf5f5f5"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ProgressBar}">
                        <Grid x:Name="TemplateRoot">
                            <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}"
                                    CornerRadius="4"/>
                            <Rectangle x:Name="PART_Track"/>
                            <Grid x:Name="PART_Indicator" ClipToBounds="True" HorizontalAlignment="Left">
                                <Rectangle x:Name="Indicator" Fill="{TemplateBinding Foreground}" RadiusX="4" RadiusY="4"/>
                            </Grid>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>
    <Grid x:Name="LayoutRoot" Background="WhiteSmoke">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <ListBox x:Name="RemoteListBox" Grid.Column="1"
                 HorizontalAlignment="Stretch" Margin="20,90,0,0" 
                 HorizontalContentAlignment="Stretch"
                 SelectedItem ="{Binding RemoteSelectedMotion}"
                 ItemsSource ="{Binding RemoteItemsList}"				 
                 SelectionChanged="RemoteListBox_SelectionChanged" 
                 SelectedIndex="0" Background="#f4f4f4"
                 BorderThickness="1,1,0,0">
            <ListBox.Style>
                <Style TargetType="{x:Type ListBox}">
                    <Style.Resources>
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#f8d699" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#3c3c3c" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="Transparent" />
                    </Style.Resources>
                </Style>
            </ListBox.Style>
            <ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="Padding" Value="0"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>



        <TextBlock x:Name="UnconnectedTextBox" Grid.Column="1"
                   HorizontalAlignment="Center" 
                   MouseDown="UnconnectedTextBox_MouseDown"  
                   TextWrapping="Wrap" Margin="20,90,0,0" 
                   Width="205.413" 
                   Panel.ZIndex="1" 
                   TextAlignment="Center" 
                   TextDecorations="Underline" 
                   RenderTransformOrigin="0.165,0.33" 
                   Height="27.627" 
                   VerticalAlignment="Center" 
                   Cursor="Hand">
            <Run Text="Please connect to robot"/>
        </TextBlock>
        <local:PreviousTrackButton x:Name="PreviousButton" 
                                   Margin="144.5,55,233,0" VerticalAlignment="Top" Width="Auto" 
                                   Height="35" Grid.Column="1" d:LayoutOverrides="GridBox" />
        <local:PlayPauseButton x:Name="PlayPauseButtons" 
                               Height="35" Margin="180.5,55,197,0" VerticalAlignment="Top" Width="Auto" Grid.Column="1" d:LayoutOverrides="GridBox"/>
        <local:NextTrackButton x:Name="NextButton" Margin="216.5,55,161,0" VerticalAlignment="Top" 
                               Width="35" Height="35" 
                               d:LayoutOverrides="GridBox" Grid.Column="1"/>
        <local:VolumeButton x:Name="VolumeButton" Margin="252.5,55,125,0" 
                            VerticalAlignment="Top" Width="Auto" Height="35" Grid.Column="1" d:LayoutOverrides="GridBox"/>
        <local:RobotTransformButton x:Name="TransformButton" 
                                    Margin="64.5,55,313,0" Width="Auto" Height="35" 
                                    VerticalAlignment="Top" Grid.Column="1" d:LayoutOverrides="GridBox"/>
        <ScrollViewer Margin="20,90,0,0" ScrollViewer.VerticalScrollBarVisibility="Auto" Grid.Column="0">
            <ScrollViewer.Resources>
                <Style TargetType="ScrollViewer">
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>                    
                </Style>
                <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource FullAppleScrollBar}"/>
            </ScrollViewer.Resources>
            <ItemsControl ItemsSource="{Binding LibraryItemsList}" HorizontalContentAlignment="Stretch"/>
        </ScrollViewer>
        <ProgressBar x:Name="volumeBar"  Height="9" Width="100"
                    Value="{Binding Volume}" Maximum="10"
                    Style="{DynamicResource VolumeStyle}" HorizontalAlignment="Right" Margin="0,69.815,8,0" VerticalAlignment="Top" d:LayoutOverrides="HorizontalAlignment, Width" 
                     Background="#cdced5" BorderBrush="#FFb6b6ba" BorderThickness="1" Grid.Column="1">
            <ProgressBar.Effect>
                <DropShadowEffect ShadowDepth="1" BlurRadius="1" Color="White"/>
            </ProgressBar.Effect>
        </ProgressBar>
        <Slider x:Name="VolumeSlider" HorizontalAlignment="Right" Margin="0,66.815,8,0" VerticalAlignment="Top" Width="100" Value="{Binding Volume}"
                IsSnapToTickEnabled="True" TickFrequency="1" IsMoveToPointEnabled="true" Style="{StaticResource VolumeSliderStyle}" Grid.Column="1" d:LayoutOverrides="HorizontalAlignment"/>
    </Grid>
</UserControl>
